#include <stdio.h>
#include <math.h>

int main (void) {
   
   printf("a raiz quadrada de %lf eh %lf\n", 4.0, sqrt(4.0) );
   printf("a raiz quadrada de %lf eh %lf\n", 5.0, sqrt(5.0) );

   return 0;
}

